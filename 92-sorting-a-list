public List<String> getNamesInAscOrder(List<String> accountNames)
{
    //code here
    List<string> sortedList = new List<string>();

    for (Integer i = 0; i < accountNames.size(); i++) {
        sortedList.add(accountNames.get(i));
    }

    sortedList.sort();

    if (sortedList.isBlank(sortedList)) {
        return null;
    } else {
        return sortedList;
    }
    
}
