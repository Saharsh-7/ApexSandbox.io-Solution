public List<String> getNamesInAscOrder(List<String> accountNames) {
    if (accountNames == null || accountNames.contains(null)) {
        return accountNames;
    } else {
        List<String> sortedList = new List<String>(accountNames);
        sortedList.sort();
        return sortedList;
    }
}
